# Spring Boot Application Properties

# Server Port
server.port=8080

# H2 Database (for development/testing)
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console
#spring.datasource.url=jdbc:h2:mem:ecolife_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
#spring.datasource.driverClassName=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=

# JPA/Hibernate Properties
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration (replace with your actual secret key)
spring.security.jwt.secret=${env:SPRING_SECURITY_JWT_SECRET}
spring.security.jwt.expiration=${env:SPRING_SECURITY_JWT_EXPIRATION}

# CORS Configuration
spring.web.cors.enabled=true
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
spring.web.cors.max-age=3600

# --- MySQL Configuration (Uncomment and configure if using MySQL) ---
spring.datasource.url=jdbc:mysql://localhost:3306/ecolife_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456789
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# --- PostgreSQL Configuration (Uncomment and configure if using PostgreSQL) ---
# spring.datasource.url=jdbc:postgresql://localhost:5432/ecolife_db
# spring.datasource.username=your_pg_username
# spring.datasource.password=your_pg_password
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update
